%h2 Articles Tagged with #{@tag.name}
%ul
  - @tag.articles.each do |article|
    %li[@tag]= link_to article.title, article_path(article)


.delete_button
  -if logged_in?
    =  link_to "Delete", tag_path(@tag), method: :delete, confirm: "Delete?" 
