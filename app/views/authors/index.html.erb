<h2>Listing authors</h2>

<table>
  <tr>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @authors.each do |author| %>
  <tr>
    <td><%= author.username %></td>
    <td></td>
    <td></td>
    <td><%= link_to 'Show', author_path(author), class: "btn" %></td>
    <td><%= link_to 'Edit', edit_author_path(author), class: "btn" %></td>
    <td><%= link_to 'Destroy', author_path(author), class: "btn", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Author', new_author_path, class: "btn" %>
